# for pnpm
shamefully-hoist=true
auto-install-peers=true

# Slidev + UnoCSS 在 monorepo 下对 iconify 集合的 require.resolve 从 .pnpm 层向上查找，
# 需要把 iconify 系列 hoist 到根 node_modules 顶层；public-hoist-pattern 按需精确暴露。
public-hoist-pattern[]=@iconify/*
public-hoist-pattern[]=@iconify-json/*
